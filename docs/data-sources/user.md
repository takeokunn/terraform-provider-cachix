---
page_title: "cachix_user Data Source - cachix"
subcategory: ""
description: |-
  Fetches information about the current authenticated Cachix user. Use this data source to retrieve details about the user associated with the configured API token.
---

# cachix_user (Data Source)

Fetches information about the current authenticated Cachix user. Use this data source to retrieve details about the user associated with the configured API token.

## Example Usage

```terraform
# Retrieve the current authenticated user
data "cachix_user" "current" {}

output "username" {
  value = data.cachix_user.current.username
}

output "email" {
  value     = data.cachix_user.current.email
  sensitive = true
}

output "subscription_plan" {
  value = data.cachix_user.current.subscription_plan
}

# Storage limit and usage are reported in bytes.
output "storage_usage_bytes" {
  value = data.cachix_user.current.subscription_storage_usage
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `email` (String, Sensitive) The email address of the authenticated user.
- `fullname` (String) The full name of the authenticated user.
- `id` (String) The identifier of the user (same as username).
- `subscription_plan` (String) The subscription plan of the authenticated user (e.g., `Community`).
- `subscription_storage_limit` (Number) The storage limit of the subscription, in bytes.
- `subscription_storage_usage` (Number) The current storage usage of the subscription, in bytes.
- `username` (String) The username of the authenticated user.
